<%= _('%{name}(%{url}) namespace has run out of compute minutes so no new jobs or pipelines in its projects will run.') % { name: @namespace.name, url: group_url(@namespace) }%>

<%= _('We recommend that you buy additional compute minutes to resume normal service.') %>

<%= _('Buy more compute minutes') %> → <%= buy_additional_minutes_url(@namespace) %>
